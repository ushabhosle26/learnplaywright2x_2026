let attempt = 0; // Init
while (attempt < 3) { // Condition
    console.log("Attempt : ",attempt);
    attempt++; // Update
}

let modi = 1;
while (modi <= 15) { // 1 to 15,Times -> 

    console.log("Modi will do 15+ years : ",modi);
    modi++;

}